name: tumejorsonrisa
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.11.0

flutter_icons:
  android: true
  ios: true
  image_path: "lib/images/fondo.jpg"  # Ruta de la imagen que deseas usar como icono

flutter:
  uses-material-design: true
  assets:
    - lib/images/fondo.jpg
